# Views

This directory contains page-level components:

- `auth/` - Authentication pages (Login, Register)
- `products/` - Product pages (List, Detail, Form)
- `orders/` - Order pages (List, Detail)
