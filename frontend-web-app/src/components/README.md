# Components

This directory contains all Vue components organized by type:

- `common/` - Reusable UI components (Button, Input, Modal, etc.)
- `layout/` - Layout components (Header, Footer, Navigation, etc.)
- `features/` - Feature-specific components (ProductCard, OrderItem, etc.)
